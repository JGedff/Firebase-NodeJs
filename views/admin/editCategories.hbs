{{#each category}}
<a href="/categories/admin/edit/{{id}}" class="btn btn-info m-1"> Edit {{name}} </a>
<button onclick="showAlertDelete({{counter}})" class="btn btn-info m-1"> Delete {{name}} </button>
<div id="alertDelete{{counter}}" class="alertDelete">
    <p>Are you sure you want to <strong class="text-red">delete</strong> this category?</p>
    <p>If you delete the category, the documents will keep the category, but the searcher will not be able to find them</p>
    <a href="/categories/admin/delete/{{id}}" class="btn btn-danger m-1"> Yes </a>
    <button onclick="hideAlertDelete({{counter}})" class="btn btn-success m-1"> No </button>
</div>
<div>
    <p>Name: <strong>{{name}}</strong></p>
    <p>Id: <strong>{{id}}</strong></p>
</div>
<hr>
{{/each}}